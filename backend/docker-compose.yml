version: "3.8"

services:
  redis:
    image: redis:alpine
    networks:
      - hyperledger

  backend:
    build: .
    depends_on:
      - redis
    ports:
      - 8000:8000
    volumes:
      - "${PWD}/network.yaml:/out/network.yaml"
    networks:
      - hyperledger
      
networks:
  hyperledger:
    name: fabric_test
    external: true
